#include <pthread.h>
#include <stdio.h>
#include <stdlib.h>
#include <cstdext/container/queue.h>
#include <cstdext/core.h>

queue *lq;

typedef struct {
  byte *data;
  u32 len;
  u32 capacity;  
  pthread_mutex_t m;
} list;



list *list_create() {
  lq = queue_create(PTR);
  list *l = make(list);


  
}
void list_destroy(list *l);

int main() {


  return 0;
}
